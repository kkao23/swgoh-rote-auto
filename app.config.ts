export default defineAppConfig({
  ui: {
    primary: 'slate',
    gray: 'cool',
    notifications: {
      // Show toasts at the top middle of the screen
      position: 'top-0 bottom-[unset] left-1/2 -translate-x-1/2'
    }
  }
})